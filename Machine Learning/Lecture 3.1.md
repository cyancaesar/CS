# Linear Neural Networks (Linear Regression)

> Regression
> A set of methods of modeling the relationship between one or more independent variables and a dependent variable.

It purpose is to characterize the relationship.

### Basic Elements of Linear Regression

- Linear Model
- Loss Function
- Analytic Solution
- Minibatch Stochastic Gradient Descent
- Making Predictions with the Learned Model

Linear Model

Linearity assumption says that the target can be expressed as a weighted sum of the features (area and age).

Hyperparameter tuning is the process by which hyperparameters are chosen, and typically requires that we adjust them based on the results of the training loop as assessed on a separate validation dataset.

